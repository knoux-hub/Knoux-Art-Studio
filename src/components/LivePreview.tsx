import React from 'react';

const LivePreview: React.FC = () => (
  <div className="live-preview">
    <h3>Live Preview</h3>
    <div className="preview-frame">Preview Area</div>
  </div>
);

export default LivePreview;
